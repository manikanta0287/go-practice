package logics

func PrintNumber(n int, Original int, K int, flag bool) {

	// print the number
	// document.write(N + " ");

	// // change flag if number
	// // become negative
	// if (n <= 0)
	// 	// flag = !flag;

	// // base condition for
	// // second_case (Adding K)
	// if (n == Original && !flag)

	// 	return;

	// // if flag is true
	// // we subtract value until
	// // number is greater than zero
	// if (flag == true) {

	// 	PrintNumber(n - K, Original,
	// 					K, flag);

	// 	return;
	// }

	// // second case (Addition )
	// if (!flag) {

	// 	PrintNumber(N + K, Original,
	// 					K, flag);

	// 	return;
	// }
}
